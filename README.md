# LearnLive

# Git Commands
git pull origin master
git checkout -b component-calendar
git add .
git commit -m "Added Calendar"
git push origin index.html

# To undo changes
git status
git checkout filepath
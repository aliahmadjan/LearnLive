# LearnLive


# Git Commands
git checkout master // for checking the branch
git pull origin master // pulling down the master branch if someone has made any commits
git checkout -b component-calendar // making new branch
git add . // ye bhi kch kerta hy
git commit -m "Added Calendar" // commit message
git push origin index.html // pushing new branch
